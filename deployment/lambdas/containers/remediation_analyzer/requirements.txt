# PDF processing dependencies
pymupdf>=1.24.0
pikepdf>=8.0.0
lxml>=5.0.0

# Foundation dependencies (must be installed from the main BADGERS foundation package)
# boto3>=1.34.0
# botocore>=1.34.0
# Pillow>=10.0.0
# strands-agents>=0.1.0

# AWS SDK (Lambda runtime provides these, but include for local development)
boto3>=1.34.0
botocore>=1.34.0
Pillow>=10.0.0

# Note: This package requires the BADGERS foundation library to be available in the Python path.
# The foundation library should be deployed alongside this package or installed separately.
